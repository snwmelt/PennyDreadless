<Page x:Class="PennyDreadless.Views.LoginPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PennyDreadless.Views"
      xmlns:vms="clr-namespace:PennyDreadless.View_Models"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPageView">

    <Page.DataContext>
        <vms:LoginPageViewModel/>
    </Page.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox x:Name="UsernameTextBox" TextAlignment="Center" VerticalContentAlignment="Center" Height="30" Width="300" Grid.Row="1" Text="{Binding Mode=TwoWay, Path=Username}"/>
        <TextBox x:Name="PasswordTextBox" TextAlignment="Center" VerticalContentAlignment="Center" Height="30" Width="300" Grid.Row="2" Text="{Binding Mode=TwoWay, Path=Password}"/>

        <Button x:Name="LoginUserButton"  Height="40" Width="280" Grid.Row="4" Content="Login" Command="{Binding Path=ValidateUserCommand}"/>
        <Button x:Name="CreateUserButton" Height="40" Width="280" Grid.Row="5" Content="Create" Command="{Binding Path=CreateUserCommand}"/>
    </Grid>
</Page>
