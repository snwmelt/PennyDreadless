<Page x:Class="PennyDreadless.Views.AccountsPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vms="clr-namespace:PennyDreadless.View_Models" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AccountsPageView">
    
    <Page.DataContext>
        <vms:AccountsPageViewModel/>
    </Page.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <ListBox>
                
            </ListBox>
            
        </Grid>

        <Grid Grid.Column="1" Width="200" Visibility="{Binding AccountDataIsVisible}">
            
        </Grid>
        
    </Grid>
</Page>
